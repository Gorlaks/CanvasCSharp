export const url = "https://localhost:5001";

export enum RoutePath {
	ROOT_PATH = "/",
	AUTH_PATH = "/auth",
	USER_PATH = "/user",
	CANVAS_PATH = "/canvas"
};


export enum UrlPath {
	LOGIN = "/login",
	USER = "/userCanvases"
};